# Direct dependencies only. Transitive packages (starlette, pydantic-core,
# anyio, h11, httpcore, ...) are resolved by pip and are not pinned here.
# All versions below support Python 3.9+.

# --- application ---
fastapi==0.128.8          # web framework: routing, validation, CORS, error handling
uvicorn==0.39.0           # ASGI server used to run the app
python-multipart==0.0.20  # required by FastAPI to parse the multipart upload on POST /upload
PyMuPDF==1.26.5           # PDF text extraction, imported as `fitz`
pydantic==2.13.4          # request/response models and the recovery-first validators
httpx==0.28.1             # async HTTP client for the local Ollama API
python-dotenv==1.2.1      # loads backend/.env

# --- testing ---
pytest==8.4.2             # test runner; httpx above also backs fastapi.testclient
