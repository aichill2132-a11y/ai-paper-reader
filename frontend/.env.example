# Base URL of the AI Paper Reader backend.
# Copy this file to .env.local to override it; the default below matches the
# backend's own default (uvicorn main:app on port 8000).
VITE_API_BASE_URL=http://127.0.0.1:8000
